import {jsonImporter} from "./json";import {pdfImporter} from "./pdf";import {CharacterImporter,ImportResult} from "./types";const importers:CharacterImporter[]=[jsonImporter,pdfImporter];export async function importCharacterFile(file:File):Promise<ImportResult>{const importer=importers.find(i=>i.supports(file));if(!importer)throw new Error("Unsupported file. Choose a fillable PDF or ADaM JSON file.");return importer.import(file)}export type {ImportFormat,ImportResult} from "./types";
